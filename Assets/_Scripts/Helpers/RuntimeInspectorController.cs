//using RuntimeInspectorNamespace;
using UnityEngine;

public class RuntimeInspectorController : MonoBehaviour
{
    /*private RuntimeInspector _inspector;
    
    [SerializeField] private ScriptableObject configToInspect;
    
    private void Awake()
    {
        _inspector = GetComponent<RuntimeInspector>();
    }
    
    private void OnEnable()
    {
        _inspector.Inspect(configToInspect);
    }*/
}
